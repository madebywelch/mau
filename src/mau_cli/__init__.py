"""MAU-CLI — Multi-Agent Unit orchestrator.

Spawns a simulated engineering team (Product → EM → Tech Lead → specialists)
that breaks down a high-level request and coordinates dependent work via
hierarchical and lateral message-passing. Inference runs through the local
`claude` or `codex` CLI.
"""

__version__ = "0.1.0"
